alias start_default='if [[ -n `screen -ls | grep default` ]] ; then `screen -dr default` ; else `screen -c ~/.screeninator/default.screen -S default`; fi'
alias start_dgenju='if [[ -n `screen -ls | grep dgenju` ]] ; then `screen -dr dgenju` ; else `screen -c ~/.screeninator/dgenju.screen -S dgenju`; fi'
alias start_genju='if [[ -n `screen -ls | grep genju` ]] ; then `screen -dr genju` ; else `screen -c ~/.screeninator/genju.screen -S genju`; fi'
alias start_gh-dev='if [[ -n `screen -ls | grep gh-dev` ]] ; then `screen -dr gh-dev` ; else `screen -c ~/.screeninator/gh-dev.screen -S gh-dev`; fi'
alias start_media='if [[ -n `screen -ls | grep media` ]] ; then `screen -dr media` ; else `screen -c ~/.screeninator/media.screen -S media`; fi'
alias start_mgenju='if [[ -n `screen -ls | grep mgenju` ]] ; then `screen -dr mgenju` ; else `screen -c ~/.screeninator/mgenju.screen -S mgenju`; fi'
alias start_nfs='if [[ -n `screen -ls | grep nfs` ]] ; then `screen -dr nfs` ; else `screen -c ~/.screeninator/nfs.screen -S nfs`; fi'
alias start_otherbu='if [[ -n `screen -ls | grep otherbu` ]] ; then `screen -dr otherbu` ; else `screen -c ~/.screeninator/otherbu.screen -S otherbu`; fi'
alias start_punk='if [[ -n `screen -ls | grep punk` ]] ; then `screen -dr punk` ; else `screen -c ~/.screeninator/punk.screen -S punk`; fi'
alias start_seishun='if [[ -n `screen -ls | grep seishun` ]] ; then `screen -dr seishun` ; else `screen -c ~/.screeninator/seishun.screen -S seishun`; fi'
alias start_sh-dev='if [[ -n `screen -ls | grep sh-dev` ]] ; then `screen -dr sh-dev` ; else `screen -c ~/.screeninator/sh-dev.screen -S sh-dev`; fi'